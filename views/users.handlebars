{{!-- {{#each users as |user|}}
{{#if comments.length}}
<section class="container w-100">
  <h2>{{username}}</h2>
  <div class="post-list row">
    {{#each comments as |comment|}}
       <div class="movie-card col-6 col-sm-3">
  <img
    src="{{movie.poster}}"
    class=""
  ></img>
  <div class="movie-review">
    <a>{{movie.title}}</a><hr>
    <a>{{#times movie_rating}}
      <i class="fas fa-star"></i>
    {{/times}}</a>
    <p>{{movie_rating}}/5</p>
    <p class="card-text">{{shortened_comment comment_text}}</p>
    <hr>
    <a href="/movie/{{movie.id}}">Check out all reviews</a><br>
  </div>
</div>
    {{/each}}
  </div>
</section>
{{/if}}
{{/each}} --}}

<div class="d-flex flex-column">
  {{#each users as |user|}}
    <h2>{{username}}</h2>
    {{#if comments.length}}
      <div class="userCardContainer w-100">
        {{#each comments as |comment|}}
          <div
            class="userCard"
            style="background-image: url('{{movie.poster}}')"
          >
            <h3 class="title">{{movie.title}}</h3>
            <div class="inner-text">
              <a>{{#times movie_rating}}
                  <i class="fas fa-star"></i>
                {{/times}}</a>
              <p>{{movie_rating}}/5</p>
              <hr />
              <p class="">{{shortened_comment comment_text}}</p>
              <a href="/movie/{{movie_id}}">Check out all reviews</a><br />
            </div>
          </div>
        {{/each}}
      </div>
    {{else}}
      <h3>This user has not reviewed anything yet.</h3>
      <h4>Guess they are just here to see what you are doing...?</h4>
    {{/if}}
  {{/each}}
</div>